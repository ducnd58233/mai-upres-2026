[project]
name = "mai-upres-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
]

[dependency-groups]
datasets = [
    "kaggle>=1.8.4",
]
dev = [
    "pre-commit>=4.5.1",
]
